const initState = {
    testValue: 'gfdgdfg'
}

const rootReducer = (state = initState, action) => {
    switch (action.tyoe) {
        case "":
            
            break;
    
        default:
            break;
    }
}

export default rootReducer;